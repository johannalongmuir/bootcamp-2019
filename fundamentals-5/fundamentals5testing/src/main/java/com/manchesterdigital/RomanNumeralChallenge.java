package com.manchesterdigital;

public class RomanNumeralChallenge {
    public static String convert(int number, String roman) {
        if (number < 4) {
            // StringBuilder roman = new StringBuilder(i);
            // roman.append(i);
            // do an array, for number that is number. Input roman into all the cells. Output should be correct

            return roman;


        }
        return "X";
    }
}
